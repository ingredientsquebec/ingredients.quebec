stages:
#  - test
  - staging
  - prod

before_script:
# SSH Prep
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - echo "$IQKEYSCAN" > ~/.ssh/known_hosts
  - chmod 644 ~/.ssh/known_hosts
  #- 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  #- eval $(ssh-agent -s)
  #- echo "$IQSSHKEY" | tr -d '\r' | ssh-add - > /dev/null


###
# Run tests.
###
#test:
#  stage: test
#  script:
#    - tox -e py36
#  tags:
#    - linux


preprod5000:
  stage: staging
  script:
    - echo -n "$IQSSHKEY" > iq.creds
    - chmod 0400 iq.creds
    - scp -o StrictHostKeyChecking=no -r -i iq.creds * ubuntu@ingredients.quebec:/tmp
#  when: on_success
  tags:
    - linux

prod:
  stage: prod
  script:
    - ssh in the box and git pull
    - restart gunicorn
  when: manual
  tags:
    - linux
